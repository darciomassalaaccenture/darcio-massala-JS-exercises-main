// Cursor Park (anyone not typing put your cursor here)

const addOne = (i) => i + 1

const one = addOne(0) // 1
const two = addOne(addOne(0)) // 2
const three = addOne(addOne(addOne(0))) // 3

//or
/*
const one = addOne(0)
const two = addOne(one)
const three = addOne(two)
*/
